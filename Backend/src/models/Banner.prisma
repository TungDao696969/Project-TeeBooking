model Banner {
    id          String   @id @default(uuid())
    title       String
    imageUrl    String
    redirectUrl String?
    startDate   DateTime
    endDate     DateTime
    isActive    Boolean  @default(true)

    @@map("banners")
}
