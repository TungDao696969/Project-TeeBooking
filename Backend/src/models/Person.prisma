model Person {
  id          String    @id @default(uuid())
  fullName    String
  avatarUrl   String?
  bio         String?
  birthDate   DateTime?
  nationality String?

  movieCasts MovieCast[]

  @@map("people")
}
